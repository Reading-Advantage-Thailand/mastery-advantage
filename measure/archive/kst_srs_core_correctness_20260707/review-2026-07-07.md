# Track Review — KST+SRS Core Algorithm Correctness (`kst-srs-v3`)

**Review date:** 2026-07-07  
**Track status:** completed (`kst-srs.v3` released)  
**Reviewer:** automated commit/spec review

---

## Summary of changes

Ten commits landed on 2026-07-07 for this track:

| Commit | Message | Key deliverable |
|---|---|---|
| `915df9b` | Phase 1 — freeze algorithm decision contracts D1–D4 | `decisions.md`, plan updates, tech-debt note |
| `8c7dd0b` | Phase 2 — author v2-vs-v3 acceptance examples E1–E5 | `examples.md` with paired numeric examples |
| `a850a7f` | FR-1 — gated weighted readiness | `SPECIFICATION.md` §2.5, §2.4 `hardGateThreshold` |
| `fd89f42` | FR-2 — condition necessity posterior on ¬A rows | `SPECIFICATION.md` §6.4 |
| `386c118` | FR-3 — retention signature + objective aggregation | `SPECIFICATION.md` §2.1.1, §13.5 |
| `18e49f0` | FR-4 — placement seeding contract + closure | `SPECIFICATION.md` §11.4, §2.3 |
| `691a822` | FR-5 — queue schedules reviews before new cards | `SPECIFICATION.md` §12.7 |
| `92a430e` | FR-6 — misconception-cap xref + symmetric progressTrend | `SPECIFICATION.md` §8.4, §9.4 |
| `9823017` | FR-7 — version bump, changelog, appendices, migration notes | `SPECIFICATION.md` header/Appendix C, `MIGRATION-v3.md` |
| `fbb8bbf` | Mark track complete | `measure/tracks.md`, `metadata.json` |

The deliverable is normative specification text and worked examples; no runtime code was changed.

---

## Spec alignment

| FR | Requirement | Status | Evidence in spec |
|---|---|---|---|
| FR-1 | Non-compensatory hard-gate readiness | **Met** | §2.5 defines `readiness(B) = gate(B) × comp(B)`, `hardGateThreshold` default 1.0 in §2.4; worked example shows unmastered w=1.0 prerequisite now yields `blocked`. |
| FR-2 | Edge-calibration update on ¬A rows only | **Met** | §6.4 updates `α` from cell `d`, `β` from cell `c`; cells `a`/`b` excluded; worked false-confirmation example embedded. |
| FR-3 | Retention signature + objective aggregation | **Met** | §13.5 two-argument `stabilityToRetention(stability, elapsedDays)`; §2.1.1 min-over-reviewed-cards rule with `reps ≥ 1` exclusion; worked example. |
| FR-4 | Placement seeding contract + closure | **Met** | §11.4 defines `S₀ = H(confidence) × masteryEstimate`, synthesized cards, provisional mastered entry, hard-edge-only inferred closure with per-hop confidence downgrade. |
| FR-5 | Daily queue ordering + backlog | **Met** | §12.7 reviews-first by predicted retention ascending, `newCardsPerDay` hard cap, backlog mode suppresses new cards; 7-day absence example embedded. |
| FR-6 | Documentation defects | **Met** | §8.4 misconception cap now cites §13.3; §9.4 `progressTrend` uses symmetric `trendThreshold` (default 3, configurable). |
| FR-7 | Version bump + migration artifacts | **Met** | Header is `kst-srs.v3.2` (cumulative), Appendix C lists `kst-srs.v3`/`srs.contract.v3`, Appendix B v3 fixture note added, `MIGRATION-v3.md` items 1–6 exist. |

All acceptance criteria in `spec.md` appear satisfied.

---

## Code / spec quality observations

- **Worked examples are normative and paired:** every algorithmic FR has a v2-vs-v3 numeric example in `examples.md` and embedded in the spec.
- **Migration property is explicit:** §2.5 notes that graphs with no `w ≥ hardGateThreshold` edges produce bit-identical output to v2, limiting adoption risk.
- **Consistency sweep recorded:** the plan claims one stale §3.4 edge-schema comment was found and fixed; no dangling references were visible in the reviewed sections.
- **Config centralization:** new thresholds (`hardGateThreshold`, `trendThreshold`) were added to the `MasteryConfig` interface rather than scattered.
- **Cross-wiring is clean:** §2.3 references placement-seeded cards, §11.4 references §2.5/§13.5, §12.7 references §13.5.

---

## Risks or blockers

1. **Curriculum/pedagogy review not performed by a human.** The approval record in `decisions.md` states the review was "approved by owner directive" and is logged as tech debt. Constants (`H` seeding multipliers, `trendThreshold`, backlog policy) need ratification before any consuming app adopts them.
2. **Downstream runtime impact is unimplemented.** `ra-math-advantage` and other consumers still run v2 logic; `MIGRATION-v3.md` exists but no code changes were made. The behavioral changes (especially queue ordering and hard-gate readiness) are user-facing and will require UI messaging ("review day").
3. **No executable test fixtures yet.** Appendix B notes the required fixture cases, but the actual fixture files were not added in these commits.

---

## Recommended next actions

1. Schedule a human curriculum/pedagogy review of the v3 constants and queue/backlog policy; close the tech-debt item.
2. Begin the `ra-math-advantage` v2→v3 migration, following the adoption order in `MIGRATION-v3.md` (signature fix → state layer → queue → placement → offline recomputation).
3. Add executable synthetic fixtures for the five v3 behavioral changes in Appendix B so the examples become regression tests.
4. Validate that `ra-math-advantage` graph edges currently authored at `w = 1.0` are intentionally hard gates; otherwise unexpected `blocked` classifications will appear.
